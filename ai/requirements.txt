# Web-фреймворк
fastapi>=0.115
uvicorn[standard]>=0.32

# LLM-обёртка: один интерфейс к OpenAI/Anthropic/Gemini/Ollama (модель задаётся в .env)
litellm>=1.55
# нужен litellm для retry-логики (num_retries)
tenacity>=8.0

# HTTP-клиент (OpenAlex, Payload REST)
httpx>=0.28

# Конфигурация из env
pydantic-settings>=2.6

# Postgres + pgvector (таблицы эмбеддингов)
psycopg[binary]>=3.2
pgvector>=0.3

# Локальные эмбеддинги (тянет torch — первая установка долгая, это нормально)
sentence-transformers>=3.3

# Карта тем (pipelines/cluster.py): UMAP-проекция + HDBSCAN (sklearn) + TF-IDF метки
umap-learn>=0.5
scikit-learn>=1.4
pytest>=8.0
